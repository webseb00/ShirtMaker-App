import React from 'react'; 
import { Typography } from '@material-ui/core';

export default function ShirtSide({ image }) {
  console.log(image);
  return (
    <Typography variant="h2">
      ShirtGraphicStyle Component
    </Typography>
  )
}