import { Typography } from '@material-ui/core';

export default function ShirtSide({ details, type }) {
  console.log(details);
  console.log(type);
  return (
    <Typography variant="h2">
      ShirtGraphic Component
    </Typography>
  )
}