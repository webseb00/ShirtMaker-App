import { Typography, Grid, Paper, FormControlLabel, Switch, Button, Divider, makeStyles } from '@material-ui/core';
import front from '../../../images/front.png';
import back from '../../../images/back.png';

export default function OrderSummary({ shirtType, imageUrl, imageStyle }) {
  return <h1>Order Summary Component</h1>;
}
